<Window x:Class="WeatherApp.View.WeatherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherApp.View"
        xmlns:vm="clr-namespace:WeatherApp.ViewModel"
        xmlns:iv="clr-namespace:WeatherApp.ViewModel.ValueConverters"
        mc:Ignorable="d"
        Title="WeatherWindow" Height="550" Width="800">
    <Window.Resources>
        <vm:WeatherVM x:Key="vm"/>
        <iv:WeatherIconConverter x:Key="IconConverter"/> 
    </Window.Resources>
    
    <Grid DataContext="{StaticResource vm}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="20" CornerRadius="0,0,20,20">
            <StackPanel>
                <TextBlock Text="위치 좌표 입력" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Foreground="#555"/>
                <UniformGrid Columns="2" Margin="0,0,0,10">
                    <StackPanel Margin="0,0,5,0">
                        <TextBlock Text="위도 (Lat)" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                        <TextBox x:Name="LatInput" Text="{Binding Lat, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="5" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Margin="5,0,0,0">
                        <TextBlock Text="경도 (Lon)" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                        <TextBox x:Name="LonInput" Text="{Binding Lon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="5" VerticalAlignment="Center"/>
                    </StackPanel>
                </UniformGrid>
                <Button Content="날씨 조회하기"
                        Command="{Binding searchCommand}"
                        Background="#2196F3" Foreground="White" 
                        Padding="10" FontWeight="Bold" BorderThickness="0"
                        >
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
        <StackPanel VerticalAlignment="Top" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0" Grid.Row="1">
            <StackPanel Margin="0,0,40,0" Width="200">
                <TextBlock Text="검색 기록" FontSize="16" FontWeight="Bold" Foreground="#555" Margin="0,0,0,15" HorizontalAlignment="Center"/>
                <Button Content="기록 삭제"
                        Command="{Binding clearHistoryCommand}"
                        Background="#2196F3" Foreground="White" 
                        Padding="5" FontWeight="Bold" BorderThickness="0"
                        >
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <ScrollViewer Height="300" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding HistoryList}" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F9F9F9" BorderBrush="#EEE" BorderThickness="1" 
                                CornerRadius="8" Padding="10" Margin="0,0,0,8">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding name}" FontWeight="Medium"/>
                                        <TextBlock Text="{Binding main.temp, StringFormat=' {0:F1}°'}" Foreground="#2196F3" Margin="10,0,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Root.name}" FontSize="28" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding Root.weather[0].description}" FontSize="20" Foreground="#666" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock Text="{Binding Root.main.temp}" FontSize="80" FontWeight="Light" Foreground="#2196F3"/>
                    <TextBlock Text="°C" FontSize="30" Foreground="#2196F3" VerticalAlignment="Top" Margin="5,15,0,0"/>
                </StackPanel>

                <Image Source="{Binding Root.weather[0].icon, Converter={StaticResource IconConverter}}" 
                       Width="100" Height="100" 
                       HorizontalAlignment="Center"/>

                <Border BorderBrush="#DDD" BorderThickness="0,1,0,0" Padding="20" Width="500">
                    <UniformGrid Columns="3">
                        <StackPanel Margin="10">
                            <TextBlock Text="체감온도" FontSize="13" Foreground="Gray" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Root.main.feels_like, StringFormat='{}{0:F1}°C'}" FontSize="18" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Margin="10">
                            <TextBlock Text="습도" FontSize="13" Foreground="Gray" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Root.main.humidity, StringFormat='{}{0:F1}%'}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Margin="10">
                            <TextBlock Text="풍속" FontSize="13" Foreground="Gray" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Root.wind.speed, StringFormat='{}{0:F1}m/s'}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </UniformGrid>
                </Border>
            </StackPanel>
        </StackPanel>
            
    </Grid>
</Window>
